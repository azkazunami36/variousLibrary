import discord from "discord.js";

function test() {
    discord.GatewayIntentBits.GuildBans
    const client = new discord.Client({intents: []})
client.on("messageCreate", msg => { msg.guild?.channels })
}
